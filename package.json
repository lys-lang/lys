{
  "name": "aureum",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "bin": "dist/bin.js",
  "scripts": {
    "test": "mocha --require source-map-support/register --reporter list",
    "prof": "node --prof ./node_modules/mocha/bin/_mocha --timeout 999999",
    "test-travis": "npm run lint && npm run build && npm run coverage && npm run test",
    "preversion": "npm run build && npm test",
    "coverage": "(`npm bin`/istanbul cover `npm bin`/_mocha || true)",
    "lint": "tslint -t msbuild src/**/*.ts",
    "build": "rm -rf dist && tsc -p tsconfig.json && tsc -p tsconfig.test.json && chmod +x dist/bin.js",
    "watch": "npm run build && chmod +x dist/bin.js && (tsc -p tsconfig.json --watch & tsc -p tsconfig.test.json --watch)"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/menduz/aureum.git"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/menduz/aureum/issues"
  },
  "homepage": "https://github.com/menduz/aureum#readme",
  "prettier": {
    "semi": true,
    "singleQuote": true,
    "printWidth": 120
  },
  "dependencies": {
    "@types/node": "^10.9.2",
    "@webassemblyjs/ast": "^1.7.6",
    "@webassemblyjs/validation": "^1.7.6",
    "@webassemblyjs/wast-printer": "^1.7.6",
    "binaryen": "^51.0.0",
    "colors": "^1.3.1",
    "ebnf": "^1.6.3",
    "source-map-support": "^0.5.9",
    "wabt": "^1.0.0"
  },
  "devDependencies": {
    "@types/chai": "^4.1.4",
    "@types/expect": "^1.20.3",
    "@types/glob": "^5.0.35",
    "chai": "^4.1.2",
    "expect": "^23.5.0",
    "glob": "^7.1.2",
    "istanbul": "^0.4.5",
    "mocha": "^5.2.0",
    "mocha-performance": "^0.1.1",
    "tslint": "^5.11.0",
    "typescript": "^3.0.3"
  }
}
