type int = ???

fun innerFunctionArgs(a: int): int = a
fun innerFunction(): int = innerFunctionArgs(3)

fun over(): int = 1
fun over(a: int): int = a

export fun outerFunction(): int = innerFunction() + over()
