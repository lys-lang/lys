val AL_BITS: i32 = 3

val AL_SIZE: i32 = 1 << AL_BITS

val AL_MASK: i32 = AL_SIZE - 1

val MAX_SIZE_32: i32 = 1 << 30

val HEAP_BASE = 0

type asd = %struct {  }

impl asd {
  #[inline]
  private fun asd$discriminant(): i64 = {
    val discriminant: i32 = asd.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): asd = %wasm {
    (call asd$discriminant)
  }
  
  fun is(a: asd): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call asd$discriminant))
  }
  
  fun ==(a: asd, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: asd, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: asd, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): asd = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

private var startOffset: i32 = (HEAP_BASE + AL_MASK) & ~AL_MASK

private var offset: i32 = startOffset

private var lastPtr: i32 = 0

private fun growMemory(pages: i32): i32 = %wasm {
  (grow_memory (get_local $pages))
}

private fun currentMemory(): i32 = %wasm {
  (current_memory)
}

private fun max(a: i32, b: i32): i32 =
  if (a > b)
    a
  else
    b
  

#[inline]
fun allocBytes(size: i32): bytes = %wasm {
  (local $allocation i32)
  (set_local $allocation (call malloc (get_local $size)))
  (i32.store (get_local $allocation) (get_local $size))
  (i64.or (i64.extend_s/i32 (get_local $allocation)) (i64.shl (i64.extend_s/i32 (i32.const 0)) (i64.const 32)))
}

#[inline]
#[export "calloc"]
#[explicit]
fun calloc(itemCount: i32, itemSize: i32): i32 = {
  val size = itemCount * itemSize
  val $ret = malloc(size)
  memset($ret, size, 0)
  $ret
}

fun malloc(size: i32): i32 = {
  if (size > 0) {
    if (size > MAX_SIZE_32) {
      panic()
    }
    val ptr = offset
    val newPtr = (ptr + size + AL_MASK) & ~AL_MASK
    val pagesBefore = currentMemory()
    if (newPtr > pagesBefore << 16) {
      val pagesNeeded = ((newPtr - ptr + 0xffff) & ~0xffff) >>> 16
      val pagesWanted = max(pagesBefore, pagesNeeded)
      if (growMemory(pagesWanted) < 0) {
        if (growMemory(pagesNeeded) < 0) {
          panic()
        }
      }
    }
    offset = newPtr
    ptr
  } else {
    0
  }
}

fun free(ptr: i32): void = {}

fun discriminant(v: ref): u64 = %wasm {
  (i64.and (i64.const 0xffffffff00000000) (get_local $v))
}

fun memcpy(from: i32, to: i32, len: i32): void = %wasm {
  (local $end i32)
  (set_local $end (i32.add (get_local $from) (get_local $len)))
  (block $exit (loop $cont (br_if $exit (i32.eq (get_local $from) (get_local $end))) (i32.store8 (get_local $to) (i32.load8_u (get_local $from))) (set_local $from (i32.add (get_local $from) (i32.const 1))) (set_local $to (i32.add (get_local $to) (i32.const 1))) (br $cont)))
}

fun memset(ptr: i32, content: i32, len: i32): void = %wasm {
  (local $end i32)
  (set_local $end (i32.add (get_local $ptr) (get_local $len)))
  (block $exit (loop $cont (br_if $exit (i32.eq (get_local $ptr) (get_local $end))) (i32.store8 (get_local $ptr) (i32.load8_u (get_local $content))) (set_local $ptr (i32.add (get_local $ptr) (i32.const 1))) (br $cont)))
}

type String = %struct { data: bytes }

impl String {
  #[inline]
  private fun String$discriminant(): i64 = {
    val discriminant: i32 = String.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(data: bytes): String = {
    var $ref = fromPointer(system::memory::calloc(1, String.^byteSize))
    property$0($ref, data)
    $ref
  }
  
  private fun fromPointer(ptr: i32): String = %wasm {
    (i64.or (call String$discriminant) (i64.extend_u/i32 (get_local $ptr)))
  }
  
  fun property_data(target: String): bytes = property$0(target)
  fun property_data(target: String, value: bytes): void = property$0(target, value)
  
  #[inline]
  private fun property$0(target: String): bytes = bytes.load(target, String.^property$0_offset)
  #[inline]
  private fun property$0(target: String, value: bytes): void = bytes.store(target, value, String.^property$0_offset)
  
  fun is(a: String): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call String$discriminant))
  }
  
  fun store(lhs: ref, rhs: String, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): String = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl String {
  fun property_length(str: String): i32 = str.data.length / 2
  
  fun +(lhs: String, rhs: String): String = {
    val lhsData = lhs.data
    val rhsData = rhs.data
    val lhsLen = lhsData.length
    val rhsLen = rhsData.length
    var newData = system::memory::allocBytes(lhsLen + rhsLen)
    system::memory::memcpy(newData.ptr, lhsData.ptr, lhsLen)
    system::memory::memcpy(newData.ptr + lhsLen, rhs.data.ptr, rhsLen)
    String(newData)
  }
  
  fun charAt(str: String, at: i32): u16 = {
    if (at > str.length) {
      panic()
    }
    u16.load(str.data.ptr as ref, at * 2)
  }
}

type AA = BB

impl AA {
  fun is(a: AA): boolean = {
    a is BB
  }
  
  fun ==(lhs: ref, rhs: ref): boolean = lhs == rhs
  
  fun !=(lhs: ref, rhs: ref): boolean = lhs != rhs
  
  fun store(lhs: ref, rhs: AA, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): AA = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl AA {
  fun as(a: AA): ref = %wasm {
    (get_local $a)
  }
}

impl BB {
  fun as(a: BB): AA = %wasm {
    (get_local $a)
  }
}

type BB = %struct {  }

impl BB {
  #[inline]
  private fun BB$discriminant(): i64 = {
    val discriminant: i32 = BB.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): BB = %wasm {
    (call BB$discriminant)
  }
  
  fun is(a: BB): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call BB$discriminant))
  }
  
  fun ==(a: BB, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: BB, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: BB, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): BB = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

type TT = %struct {  }

impl TT {
  #[inline]
  private fun TT$discriminant(): i64 = {
    val discriminant: i32 = TT.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): TT = %wasm {
    (call TT$discriminant)
  }
  
  fun is(a: TT): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call TT$discriminant))
  }
  
  fun ==(a: TT, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: TT, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: TT, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): TT = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl BB {
  fun a(): i32 = 1
}

fun main(): i32 = BB.a()
fun main(color: Color): i32 = color.r * 0x10000 | asd | color.g * 0x100 | color.b

type test = %struct {  }

impl test {
  fun x(): i32 = 1
}

fun x(a): i32 = 1

fun x1(): i32 = 1 + 2 / 3 * 4 - -5
fun x1(): f32 = {
  fun Y(): f32 = 1.0
  Y()
}

fun x2(x): i32 = ~x
fun x2(): i32 = {
  var n = 1
  fun Y(): i32 = n
  Y()
}

fun x3(x): i32 = -x

fun x4(): i32 = ~99

fun x5(): i32 = ~test.x()

fun x6(): boolean = !true

type Tree = (Empty | Node)

impl Tree {
  fun is(a: Tree): boolean = {
    a is Empty || a is Node
  }
  
  fun ==(lhs: ref, rhs: ref): boolean = lhs == rhs
  
  fun !=(lhs: ref, rhs: ref): boolean = lhs != rhs
  
  fun store(lhs: ref, rhs: Tree, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Tree = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Tree {
  fun as(a: Tree): ref = %wasm {
    (get_local $a)
  }
}

impl Empty {
  fun as(a: Empty): Tree = %wasm {
    (get_local $a)
  }
}

impl Node {
  fun as(a: Node): Tree = %wasm {
    (get_local $a)
  }
}

type Empty = %struct {  }

type Node = %struct { a: (Tree | Forest) }

impl Empty {
  #[inline]
  private fun Empty$discriminant(): i64 = {
    val discriminant: i32 = Empty.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): Empty = %wasm {
    (call Empty$discriminant)
  }
  
  fun is(a: Empty): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Empty$discriminant))
  }
  
  fun ==(a: Empty, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: Empty, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: Empty, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Empty = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Node {
  #[inline]
  private fun Node$discriminant(): i64 = {
    val discriminant: i32 = Node.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(a: ((Tree | Forest))): Node = {
    var $ref = fromPointer(system::memory::calloc(1, Node.^byteSize))
    property$0($ref, a)
    $ref
  }
  
  private fun fromPointer(ptr: i32): Node = %wasm {
    (i64.or (call Node$discriminant) (i64.extend_u/i32 (get_local $ptr)))
  }
  
  fun property_a(target: Node): ((Tree | Forest)) = property$0(target)
  fun property_a(target: Node, value: ((Tree | Forest))): void = property$0(target, value)
  
  #[inline]
  private fun property$0(target: Node, value: ((Tree | Forest))): void = storePropertyWithOffset$0(target, value, Node.^property$0_offset)
  #[inline]
  private fun property$0(target: Node): ((Tree | Forest)) = loadPropertyWithOffset$0(target, Node.^property$0_offset)
  
  #[inline]
  private fun loadPropertyWithOffset$0(target: Node, offset: i32): ((Tree | Forest)) = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $target))))
  }
  
  #[inline]
  private fun storePropertyWithOffset$0(target: Node, value: ((Tree | Forest)), offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $target))) (get_local $value))
  }
  
  fun is(a: Node): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Node$discriminant))
  }
  
  fun store(lhs: ref, rhs: Node, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Node = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

type Forest = (Nil | Cons)

impl Forest {
  fun is(a: Forest): boolean = {
    a is Nil || a is Cons
  }
  
  fun ==(lhs: ref, rhs: ref): boolean = lhs == rhs
  
  fun !=(lhs: ref, rhs: ref): boolean = lhs != rhs
  
  fun store(lhs: ref, rhs: Forest, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Forest = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Forest {
  fun as(a: Forest): ref = %wasm {
    (get_local $a)
  }
}

impl Nil {
  fun as(a: Nil): Forest = %wasm {
    (get_local $a)
  }
}

impl Cons {
  fun as(a: Cons): Forest = %wasm {
    (get_local $a)
  }
}

type Nil = %struct {  }

type Cons = %struct { tree: (Tree | Forest) }

impl Nil {
  #[inline]
  private fun Nil$discriminant(): i64 = {
    val discriminant: i32 = Nil.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): Nil = %wasm {
    (call Nil$discriminant)
  }
  
  fun is(a: Nil): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Nil$discriminant))
  }
  
  fun ==(a: Nil, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: Nil, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: Nil, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Nil = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Cons {
  #[inline]
  private fun Cons$discriminant(): i64 = {
    val discriminant: i32 = Cons.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(tree: ((Tree | Forest))): Cons = {
    var $ref = fromPointer(system::memory::calloc(1, Cons.^byteSize))
    property$0($ref, tree)
    $ref
  }
  
  private fun fromPointer(ptr: i32): Cons = %wasm {
    (i64.or (call Cons$discriminant) (i64.extend_u/i32 (get_local $ptr)))
  }
  
  fun property_tree(target: Cons): ((Tree | Forest)) = property$0(target)
  fun property_tree(target: Cons, value: ((Tree | Forest))): void = property$0(target, value)
  
  #[inline]
  private fun property$0(target: Cons, value: ((Tree | Forest))): void = storePropertyWithOffset$0(target, value, Cons.^property$0_offset)
  #[inline]
  private fun property$0(target: Cons): ((Tree | Forest)) = loadPropertyWithOffset$0(target, Cons.^property$0_offset)
  
  #[inline]
  private fun loadPropertyWithOffset$0(target: Cons, offset: i32): ((Tree | Forest)) = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $target))))
  }
  
  #[inline]
  private fun storePropertyWithOffset$0(target: Cons, value: ((Tree | Forest)), offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $target))) (get_local $value))
  }
  
  fun is(a: Cons): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Cons$discriminant))
  }
  
  fun store(lhs: ref, rhs: Cons, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Cons = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

fun testPassing(): void = {
  var a = Nil
  var b = Cons(Empty)
  var c = Cons(Nil)
  var d = Cons(Node(Empty))
  var e = Node(Nil)
  support::test::assert(a is Nil == true)
  support::test::assert(a is Forest == true)
  support::test::assert(b is Forest == true)
  support::test::assert(c is Cons == true)
  support::test::assert(e is Node == true)
  support::test::assert(e is Tree == true)
}

fun testFn(): void = {
  var x = 1
  loop {
    x = x + 1
    if (x > 10)
      break
    
  }
}

fun testFn2(): void = {
  var x = 1
  loop {
    x = x + 1
    continue
  }
}

type Color = %struct { r: i32, g: i32, b: i32 }

impl Color {
  #[inline]
  private fun Color$discriminant(): i64 = {
    val discriminant: i32 = Color.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(r: i32, g: i32, b: i32): Color = {
    var $ref = fromPointer(system::memory::calloc(1, Color.^byteSize))
    property$0($ref, r)
    property$1($ref, g)
    property$2($ref, b)
    $ref
  }
  
  private fun fromPointer(ptr: i32): Color = %wasm {
    (i64.or (call Color$discriminant) (i64.extend_u/i32 (get_local $ptr)))
  }
  
  fun property_r(target: Color): i32 = property$0(target)
  fun property_r(target: Color, value: i32): void = property$0(target, value)
  
  #[inline]
  private fun property$0(target: Color): i32 = i32.load(target, Color.^property$0_offset)
  #[inline]
  private fun property$0(target: Color, value: i32): void = i32.store(target, value, Color.^property$0_offset)
  
  fun property_g(target: Color): i32 = property$1(target)
  fun property_g(target: Color, value: i32): void = property$1(target, value)
  
  #[inline]
  private fun property$1(target: Color): i32 = i32.load(target, Color.^property$1_offset)
  #[inline]
  private fun property$1(target: Color, value: i32): void = i32.store(target, value, Color.^property$1_offset)
  
  fun property_b(target: Color): i32 = property$2(target)
  fun property_b(target: Color, value: i32): void = property$2(target, value)
  
  #[inline]
  private fun property$2(target: Color): i32 = i32.load(target, Color.^property$2_offset)
  #[inline]
  private fun property$2(target: Color, value: i32): void = i32.store(target, value, Color.^property$2_offset)
  
  fun is(a: Color): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Color$discriminant))
  }
  
  fun store(lhs: ref, rhs: Color, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Color = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

type True = %struct {  }

impl True {
  #[inline]
  private fun True$discriminant(): i64 = {
    val discriminant: i32 = True.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): True = %wasm {
    (call True$discriminant)
  }
  
  fun is(a: True): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call True$discriminant))
  }
  
  fun ==(a: True, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: True, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: True, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): True = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

type False = %struct {  }

impl False {
  #[inline]
  private fun False$discriminant(): i64 = {
    val discriminant: i32 = False.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): False = %wasm {
    (call False$discriminant)
  }
  
  fun is(a: False): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call False$discriminant))
  }
  
  fun ==(a: False, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: False, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: False, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): False = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

type Boolean = (True | False)

impl Boolean {
  fun is(a: Boolean): boolean = {
    a is True || a is False
  }
  
  fun ==(lhs: ref, rhs: ref): boolean = lhs == rhs
  
  fun !=(lhs: ref, rhs: ref): boolean = lhs != rhs
  
  fun store(lhs: ref, rhs: Boolean, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Boolean = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Boolean {
  fun as(a: Boolean): ref = %wasm {
    (get_local $a)
  }
}

impl True {
  fun as(a: True): Boolean = %wasm {
    (get_local $a)
  }
}

impl False {
  fun as(a: False): Boolean = %wasm {
    (get_local $a)
  }
}

fun gt0(x: i32): Boolean =
  if (x > 0)
    True()
  else
    False()
  

fun isTrue(a: Boolean): boolean =
  match a {
    case is True -> true
    else -> false
  }

type Iterator = %struct { current: i32, target: i32 }

impl Iterator {
  #[inline]
  private fun Iterator$discriminant(): i64 = {
    val discriminant: i32 = Iterator.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(current: i32, target: i32): Iterator = {
    var $ref = fromPointer(system::memory::calloc(1, Iterator.^byteSize))
    property$0($ref, current)
    property$1($ref, target)
    $ref
  }
  
  private fun fromPointer(ptr: i32): Iterator = %wasm {
    (i64.or (call Iterator$discriminant) (i64.extend_u/i32 (get_local $ptr)))
  }
  
  fun property_current(target: Iterator): i32 = property$0(target)
  fun property_current(target: Iterator, value: i32): void = property$0(target, value)
  
  #[inline]
  private fun property$0(target: Iterator): i32 = i32.load(target, Iterator.^property$0_offset)
  #[inline]
  private fun property$0(target: Iterator, value: i32): void = i32.store(target, value, Iterator.^property$0_offset)
  
  fun property_target(target: Iterator): i32 = property$1(target)
  fun property_target(target: Iterator, value: i32): void = property$1(target, value)
  
  #[inline]
  private fun property$1(target: Iterator): i32 = i32.load(target, Iterator.^property$1_offset)
  #[inline]
  private fun property$1(target: Iterator, value: i32): void = i32.store(target, value, Iterator.^property$1_offset)
  
  fun is(a: Iterator): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Iterator$discriminant))
  }
  
  fun store(lhs: ref, rhs: Iterator, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Iterator = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

type Maybe = (None | Some)

impl Maybe {
  fun is(a: Maybe): boolean = {
    a is None || a is Some
  }
  
  fun ==(lhs: ref, rhs: ref): boolean = lhs == rhs
  
  fun !=(lhs: ref, rhs: ref): boolean = lhs != rhs
  
  fun store(lhs: ref, rhs: Maybe, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Maybe = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Maybe {
  fun as(a: Maybe): ref = %wasm {
    (get_local $a)
  }
}

impl None {
  fun as(a: None): Maybe = %wasm {
    (get_local $a)
  }
}

impl Some {
  fun as(a: Some): Maybe = %wasm {
    (get_local $a)
  }
}

type None = %struct {  }

type Some = %struct { value: i32 }

impl None {
  #[inline]
  private fun None$discriminant(): i64 = {
    val discriminant: i32 = None.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(): None = %wasm {
    (call None$discriminant)
  }
  
  fun is(a: None): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call None$discriminant))
  }
  
  fun ==(a: None, b: ref): boolean = %wasm {
    (i64.eq (get_local $a) (get_local $b))
  }
  
  fun !=(a: None, b: ref): boolean = %wasm {
    (i64.ne (get_local $a) (get_local $b))
  }
  
  fun store(lhs: ref, rhs: None, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): None = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Some {
  #[inline]
  private fun Some$discriminant(): i64 = {
    val discriminant: i32 = Some.^discriminant
    discriminant as i64 << 32
  }
  
  fun apply(value: i32): Some = {
    var $ref = fromPointer(system::memory::calloc(1, Some.^byteSize))
    property$0($ref, value)
    $ref
  }
  
  private fun fromPointer(ptr: i32): Some = %wasm {
    (i64.or (call Some$discriminant) (i64.extend_u/i32 (get_local $ptr)))
  }
  
  fun property_value(target: Some): i32 = property$0(target)
  fun property_value(target: Some, value: i32): void = property$0(target, value)
  
  #[inline]
  private fun property$0(target: Some): i32 = i32.load(target, Some.^property$0_offset)
  #[inline]
  private fun property$0(target: Some, value: i32): void = i32.store(target, value, Some.^property$0_offset)
  
  fun is(a: Some): boolean = %wasm {
    (i64.eq (i64.and (i64.const 0xffffffff00000000) (get_local $a)) (call Some$discriminant))
  }
  
  fun store(lhs: ref, rhs: Some, offset: i32): void = %wasm {
    (i64.store (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))) (get_local $rhs))
  }
  
  fun load(lhs: ref, offset: i32): Some = %wasm {
    (i64.load (i32.add (get_local $offset) (call addressFromRef (get_local $lhs))))
  }
}

impl Iterator {
  fun next(iter: Iterator): Maybe = {
    if (iter.current <= iter.target) {
      var r = Some(iter.current)
      iter.current = iter.current + 1
      r
    } else {
      None
    }
  }
}

fun testFn3(from: i32, to: i32): i32 = {
  var ret = 0
  val $iter$ = Iterator(from, to)
  loop {
    match Iterator.next($iter$) {
      case is Some -> {
        val value = $.value
        ret = ret + value
        continue
      }
      case is None -> break
    }
  }
  ret
}